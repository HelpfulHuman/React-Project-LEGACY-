import React from 'react';

class Dashboard extends React.Component {

  static propTypes = {
  };

  static defaultProps = {
  };

  render () {
    return (
      <div>
        Dashboard
      </div>
    );
  }

}

export default Dashboard;
