import React, { Component } from 'react';

export default class Dashboard extends Component {

  static propTypes = {
  };

  static defaultProps = {
  };

  render () {
    return (
      <div>
        Dashboard
      </div>
    );
  }
}
